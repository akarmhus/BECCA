edit.var <- function(x, settings) {
  
  # Function to edit variables in new uploaded dataset at user command.
  
  # x <- name as variable to edit as character string.
  # settings <- a list containing new settings for x.
  
  
}